name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9.0.0

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm --filter @tabula-lens/node --filter @tabula-lens/react run check-types

      - name: Lint
        run: pnpm --filter @tabula-lens/node --filter @tabula-lens/react run lint

      - name: Test
        run: pnpm --filter @tabula-lens/node --filter @tabula-lens/react run test

      - name: Build
        run: pnpm --filter @tabula-lens/node --filter @tabula-lens/react run build